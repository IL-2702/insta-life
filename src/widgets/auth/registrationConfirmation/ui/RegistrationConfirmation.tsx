import { memo } from 'react'

import { RegistrationConfirmationProps } from '@/widgets/auth/registrationConfirmation/container'

export const RegistrationConfirmation = memo(({}: RegistrationConfirmationProps) => {
  return <></>
})
